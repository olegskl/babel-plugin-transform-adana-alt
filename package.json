{
  "name": "babel-plugin-transform-adana-alt",
  "version": "0.0.5",
  "description": "Like Istanbul, but better.",
  "main": "dist/plugin.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "prepublish": "babel --source-maps --out-dir dist src/",
    "lint": "eslint {src,test}/**/*.js",
    "test": "babel-tape-runner test/spec/*.spec.js"
  },
  "keywords": [
    "coverage"
  ],
  "author": "Oleg Sklyanchuk <sklyanchuk@gmail.com> (http://olegskl.com)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.4",
    "babel-plugin-transform-class-properties": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-tape-runner": "^2.0.1",
    "eslint": "^2.6.0",
    "eslint-config-meetic": "^2.0.0",
    "tape": "^4.5.1"
  },
  "dependencies": {
    "babel-template": "^6.6.0"
  }
}
