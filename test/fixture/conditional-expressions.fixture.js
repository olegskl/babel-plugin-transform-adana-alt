/* eslint-disable no-unused-expressions, no-constant-condition */

// one statement, one expression
true ? // one expression (executed once)
  1 : // one expression (executed once)
  2; // one expression (not executed)
