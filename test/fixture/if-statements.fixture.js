/* eslint-disable no-constant-condition, no-empty */
if (true) { // one test expression
  // branch (taken)
} else if (false) { // test expression
  // branch (not taken)
} else {
  // branch (cannot be taken)
}
