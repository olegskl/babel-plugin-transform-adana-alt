/* eslint-disable no-empty */
switch ('c') { // one statement, one expression
case 'a': // one test expression, one statement
case 'b': // one test expression, one statement
default: // one statement
}
